package com.example.flow.Module

import java.lang.Exception

class Repository(private val retrofit: RetrofitObject) {

//    fun login(loginInfo: LoginData): String {
//        return try {
//            retrofit.getRetrofitService().login(loginInfo).map { it.id }.toString()
//        } catch (e: Exception) { e.toString() }
//    }

}